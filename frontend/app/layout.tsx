import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Encurtador de URL - Encurte e Gerencie seus Links",
  description: "Transforme URLs longas em links curtos e fáceis de compartilhar. Sistema completo com estatísticas e gerenciamento de URLs.",
  keywords: "encurtador, url, link, curto, compartilhar, estatísticas",
  authors: [{ name: "Felipe Casagrandi" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
